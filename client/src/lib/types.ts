export interface UserProfile {
  id: string;
  name: string;
  email: string;
}

export interface ProjectType {
  name: string;
  _id: string;
  description: string;
  url: string;
  createdAt: string;
  updatedAt: string;
}
